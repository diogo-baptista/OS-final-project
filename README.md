# OS final project in C
## Objective
Created a file system in C, using threading and locks that allows the following operations:
● Create, search, remove and rename file/directory
● Open, read, write and close a file
